apiVersion: v1
kind: Service
metadata:
  name: bob-ai-agent
  labels:
    app: bob-ai-agent
spec:
  selector:
    app: bob-ai-agent
  ports:
  - name: http
    port: 3000
    targetPort: 3000
    protocol: TCP
  type: ClusterIP

# Made with Bob
